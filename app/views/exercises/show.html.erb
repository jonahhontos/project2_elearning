<h1><%= @exercise.concept.name %></h1>
<h2><%= @exercise.name %></h2>
<div class="exercise-content">
  <%= @exercise.content.html_safe %>
</div>
<div class="exercise-answer">
  <%= @exercise.answer %>
</div>
<% if session[:instructor_id] == @exercise.concept.course.instructor.id %>
  <%= link_to "Edit", edit_exercise_path(@exercise) %><br>
<% end %>
<%= link_to "Back", concept_path(@exercise.concept) %>
